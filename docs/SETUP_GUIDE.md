# Setup Guide

## Requirements

- Node.js 18+
- npm
- Optional: Supabase CLI

## Local run

```bash
npm install
npm run seed
npm run dev
```

## Notes

The app uses a JSON file-backed data store in .data/loyalty-store.json for local development. This keeps the MVP fast to run without needing a live database connection.
